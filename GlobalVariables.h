#pragma once

#include <SFML/Graphics.hpp>
#include <list>
#include <memory>

const sf::Vector2i ZERO(0, 0);
const float PI = 3.14159f;

const sf::Vector2i WINDOW_SIZE(1280, 720);
const sf::Vector2i CENTRE(WINDOW_SIZE.x / 2, WINDOW_SIZE.y / 2);

const float PLANETSIZE = 100.0f;

